include mylib.bold.a;
include another_lib.b;
include a_text_file;

let main { 
	let x = 12345;
	let y = x;
	let z = 'some "string" here';
	let section_name = "cooking";
	let imported_section = a_text_file.imported_section();

	let a_thing = my_function();
	thing();

	let b: body {
		return("
			\begin{document}
			<body>
			\end{document}
		");
	}

	let header: body {
		return "<imported_section>
	}

	return("
		\bold{I want to thank everyone}
		<header(
		)>
		<body(
			'Hi there'
		)>
	");
}
